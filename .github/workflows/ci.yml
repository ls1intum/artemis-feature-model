name: CI

on:
  push:
    branches:
      - '**'
      - '!deployment/image-publish-test'
  pull_request:
    branches:
      - '**'

permissions:
  contents: read

jobs:
  model-delivery-gates:
    name: Model delivery gates
    uses: ./.github/workflows/model-delivery-validation.yml
    permissions:
      contents: read
