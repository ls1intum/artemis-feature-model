/**
 * Storage abstraction for the catalog module. Hosts FeatureModelStore
 * and its JSON-backed MVP implementation. A future database-backed
 * implementation can replace JsonFeatureModelStore without touching the
 * REST or frontend contracts.
 */
package de.tum.cit.aet.artemis.featuremodel.catalog.repository;
