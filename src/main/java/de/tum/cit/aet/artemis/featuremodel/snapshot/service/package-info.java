/**
 * Snapshot management service. Validates imported snapshot folders, registers them under the local data root, and
 * exports imported snapshots as deterministic archives.
 */
package de.tum.cit.aet.artemis.featuremodel.snapshot.service;
