/**
 * Catalog domain module. Owns the feature model catalog itself:
 * model metadata, feature nodes, relations, constraints, source evidence,
 * and model version metadata. Hosts the storage abstraction
 * (FeatureModelStore) and the JSON-backed MVP implementation.
 */
package de.tum.cit.aet.artemis.featuremodel.catalog;
