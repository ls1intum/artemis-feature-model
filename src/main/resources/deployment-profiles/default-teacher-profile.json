{
  "id": "default-teacher-profile",
  "name": "Default Teacher Deployment Profile",
  "version": "1.0.0",
  "status": "published",
  "editableBy": [
    "maintainer"
  ],
  "providedCapabilities": [
    "default-database",
    "default-authentication",
    "default-ci-provider"
  ],
  "parameters": {
    "database.type": "postgresql",
    "deployment.mode": "docker-compose",
    "ci.provider": "localci"
  },
  "unresolvedNotes": [
    "Baseline parameters (database.type, deployment.mode, ci.provider) follow the roadmap v3 default-teacher-profile example and ordinary Artemis core defaults. They are not yet consumed by any artifactMapping valueFromProfile entry; Phase 5 must confirm which keys the configuration artifacts read."
  ]
}
