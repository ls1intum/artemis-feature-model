---
# Secret values are never stored in this package; every value below is a Vault lookup
# expression. See README.md for the Vault setup and the no-Vault alternative.
artemis_database_password: "{{ lookup('hashi_vault', 'kv/data/artemis/test/artemis-local').get('db_password') }}"
artemis_internal_admin_password: "{{ lookup('hashi_vault', 'kv/data/artemis/test/artemis-local').get('internal_admin') }}"
artemis_jhipster_jwt: "{{ lookup('hashi_vault', 'kv/data/artemis/test/artemis-local').get('jhipster_jwt') }}"